VITE_RPC_URL=http://127.0.0.1:8899
VITE_CHAIN=solana:devnet

# To point at devnet instead of the local validator, first deploy the program
# there (anchor deploy --provider.cluster devnet, from inside the container),
# then swap VITE_RPC_URL:
# VITE_RPC_URL=https://api.devnet.solana.com
