/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlyAccount,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  getAddressFromResolvedInstructionAccount,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { findSubscriptionPda } from "../pdas";
import { APP_PROGRAM_ADDRESS } from "../programs";

export const SET_MAX_AMOUNT_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  226, 133, 252, 54, 185, 15, 122, 154,
]);

export function getSetMaxAmountDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    SET_MAX_AMOUNT_DISCRIMINATOR,
  );
}

export type SetMaxAmountInstruction<
  TProgram extends string = typeof APP_PROGRAM_ADDRESS,
  TAccountSubscriber extends string | AccountMeta<string> = string,
  TAccountPlan extends string | AccountMeta<string> = string,
  TAccountSubscription extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountSubscriber extends string
        ? ReadonlySignerAccount<TAccountSubscriber> &
            AccountSignerMeta<TAccountSubscriber>
        : TAccountSubscriber,
      TAccountPlan extends string
        ? ReadonlyAccount<TAccountPlan>
        : TAccountPlan,
      TAccountSubscription extends string
        ? WritableAccount<TAccountSubscription>
        : TAccountSubscription,
      ...TRemainingAccounts,
    ]
  >;

export type SetMaxAmountInstructionData = {
  discriminator: ReadonlyUint8Array;
  newMax: bigint;
};

export type SetMaxAmountInstructionDataArgs = { newMax: number | bigint };

export function getSetMaxAmountInstructionDataEncoder(): FixedSizeEncoder<SetMaxAmountInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["newMax", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: SET_MAX_AMOUNT_DISCRIMINATOR }),
  );
}

export function getSetMaxAmountInstructionDataDecoder(): FixedSizeDecoder<SetMaxAmountInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["newMax", getU64Decoder()],
  ]);
}

export function getSetMaxAmountInstructionDataCodec(): FixedSizeCodec<
  SetMaxAmountInstructionDataArgs,
  SetMaxAmountInstructionData
> {
  return combineCodec(
    getSetMaxAmountInstructionDataEncoder(),
    getSetMaxAmountInstructionDataDecoder(),
  );
}

export type SetMaxAmountAsyncInput<
  TAccountSubscriber extends string = string,
  TAccountPlan extends string = string,
  TAccountSubscription extends string = string,
> = {
  subscriber: TransactionSigner<TAccountSubscriber>;
  plan: Address<TAccountPlan>;
  subscription?: Address<TAccountSubscription>;
  newMax: SetMaxAmountInstructionDataArgs["newMax"];
};

export async function getSetMaxAmountInstructionAsync<
  TAccountSubscriber extends string,
  TAccountPlan extends string,
  TAccountSubscription extends string,
  TProgramAddress extends Address = typeof APP_PROGRAM_ADDRESS,
>(
  input: SetMaxAmountAsyncInput<
    TAccountSubscriber,
    TAccountPlan,
    TAccountSubscription
  >,
  config?: { programAddress?: TProgramAddress },
): Promise<
  SetMaxAmountInstruction<
    TProgramAddress,
    TAccountSubscriber,
    TAccountPlan,
    TAccountSubscription
  >
> {
  // Program address.
  const programAddress = config?.programAddress ?? APP_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    subscriber: { value: input.subscriber ?? null, isWritable: false },
    plan: { value: input.plan ?? null, isWritable: false },
    subscription: { value: input.subscription ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  // Resolve default values.
  if (!accounts.subscription.value) {
    accounts.subscription.value = await findSubscriptionPda({
      plan: getAddressFromResolvedInstructionAccount(
        "plan",
        accounts.plan.value,
      ),
      subscriber: getAddressFromResolvedInstructionAccount(
        "subscriber",
        accounts.subscriber.value,
      ),
    });
  }

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("subscriber", accounts.subscriber),
      getAccountMeta("plan", accounts.plan),
      getAccountMeta("subscription", accounts.subscription),
    ],
    data: getSetMaxAmountInstructionDataEncoder().encode(
      args as SetMaxAmountInstructionDataArgs,
    ),
    programAddress,
  } as SetMaxAmountInstruction<
    TProgramAddress,
    TAccountSubscriber,
    TAccountPlan,
    TAccountSubscription
  >);
}

export type SetMaxAmountInput<
  TAccountSubscriber extends string = string,
  TAccountPlan extends string = string,
  TAccountSubscription extends string = string,
> = {
  subscriber: TransactionSigner<TAccountSubscriber>;
  plan: Address<TAccountPlan>;
  subscription: Address<TAccountSubscription>;
  newMax: SetMaxAmountInstructionDataArgs["newMax"];
};

export function getSetMaxAmountInstruction<
  TAccountSubscriber extends string,
  TAccountPlan extends string,
  TAccountSubscription extends string,
  TProgramAddress extends Address = typeof APP_PROGRAM_ADDRESS,
>(
  input: SetMaxAmountInput<
    TAccountSubscriber,
    TAccountPlan,
    TAccountSubscription
  >,
  config?: { programAddress?: TProgramAddress },
): SetMaxAmountInstruction<
  TProgramAddress,
  TAccountSubscriber,
  TAccountPlan,
  TAccountSubscription
> {
  // Program address.
  const programAddress = config?.programAddress ?? APP_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    subscriber: { value: input.subscriber ?? null, isWritable: false },
    plan: { value: input.plan ?? null, isWritable: false },
    subscription: { value: input.subscription ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("subscriber", accounts.subscriber),
      getAccountMeta("plan", accounts.plan),
      getAccountMeta("subscription", accounts.subscription),
    ],
    data: getSetMaxAmountInstructionDataEncoder().encode(
      args as SetMaxAmountInstructionDataArgs,
    ),
    programAddress,
  } as SetMaxAmountInstruction<
    TProgramAddress,
    TAccountSubscriber,
    TAccountPlan,
    TAccountSubscription
  >);
}

export type ParsedSetMaxAmountInstruction<
  TProgram extends string = typeof APP_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    subscriber: TAccountMetas[0];
    plan: TAccountMetas[1];
    subscription: TAccountMetas[2];
  };
  data: SetMaxAmountInstructionData;
};

export function parseSetMaxAmountInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedSetMaxAmountInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 3) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 3,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: {
      subscriber: getNextAccount(),
      plan: getNextAccount(),
      subscription: getNextAccount(),
    },
    data: getSetMaxAmountInstructionDataDecoder().decode(instruction.data),
  };
}
