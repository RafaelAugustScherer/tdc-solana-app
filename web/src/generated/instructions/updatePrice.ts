/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */

import {
  combineCodec,
  fixDecoderSize,
  fixEncoderSize,
  getBytesDecoder,
  getBytesEncoder,
  getStructDecoder,
  getStructEncoder,
  getU64Decoder,
  getU64Encoder,
  SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
  SolanaError,
  transformEncoder,
  type AccountMeta,
  type AccountSignerMeta,
  type Address,
  type FixedSizeCodec,
  type FixedSizeDecoder,
  type FixedSizeEncoder,
  type Instruction,
  type InstructionWithAccounts,
  type InstructionWithData,
  type ReadonlySignerAccount,
  type ReadonlyUint8Array,
  type TransactionSigner,
  type WritableAccount,
} from "@solana/kit";
import {
  getAccountMetaFactory,
  type ResolvedInstructionAccount,
} from "@solana/program-client-core";
import { APP_PROGRAM_ADDRESS } from "../programs";

export const UPDATE_PRICE_DISCRIMINATOR: ReadonlyUint8Array = new Uint8Array([
  61, 34, 117, 155, 75, 34, 123, 208,
]);

export function getUpdatePriceDiscriminatorBytes(): ReadonlyUint8Array {
  return fixEncoderSize(getBytesEncoder(), 8).encode(
    UPDATE_PRICE_DISCRIMINATOR,
  );
}

export type UpdatePriceInstruction<
  TProgram extends string = typeof APP_PROGRAM_ADDRESS,
  TAccountMerchant extends string | AccountMeta<string> = string,
  TAccountPlan extends string | AccountMeta<string> = string,
  TRemainingAccounts extends readonly AccountMeta<string>[] = [],
> = Instruction<TProgram> &
  InstructionWithData<ReadonlyUint8Array> &
  InstructionWithAccounts<
    [
      TAccountMerchant extends string
        ? ReadonlySignerAccount<TAccountMerchant> &
            AccountSignerMeta<TAccountMerchant>
        : TAccountMerchant,
      TAccountPlan extends string
        ? WritableAccount<TAccountPlan>
        : TAccountPlan,
      ...TRemainingAccounts,
    ]
  >;

export type UpdatePriceInstructionData = {
  discriminator: ReadonlyUint8Array;
  newAmount: bigint;
};

export type UpdatePriceInstructionDataArgs = { newAmount: number | bigint };

export function getUpdatePriceInstructionDataEncoder(): FixedSizeEncoder<UpdatePriceInstructionDataArgs> {
  return transformEncoder(
    getStructEncoder([
      ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
      ["newAmount", getU64Encoder()],
    ]),
    (value) => ({ ...value, discriminator: UPDATE_PRICE_DISCRIMINATOR }),
  );
}

export function getUpdatePriceInstructionDataDecoder(): FixedSizeDecoder<UpdatePriceInstructionData> {
  return getStructDecoder([
    ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ["newAmount", getU64Decoder()],
  ]);
}

export function getUpdatePriceInstructionDataCodec(): FixedSizeCodec<
  UpdatePriceInstructionDataArgs,
  UpdatePriceInstructionData
> {
  return combineCodec(
    getUpdatePriceInstructionDataEncoder(),
    getUpdatePriceInstructionDataDecoder(),
  );
}

export type UpdatePriceInput<
  TAccountMerchant extends string = string,
  TAccountPlan extends string = string,
> = {
  merchant: TransactionSigner<TAccountMerchant>;
  plan: Address<TAccountPlan>;
  newAmount: UpdatePriceInstructionDataArgs["newAmount"];
};

export function getUpdatePriceInstruction<
  TAccountMerchant extends string,
  TAccountPlan extends string,
  TProgramAddress extends Address = typeof APP_PROGRAM_ADDRESS,
>(
  input: UpdatePriceInput<TAccountMerchant, TAccountPlan>,
  config?: { programAddress?: TProgramAddress },
): UpdatePriceInstruction<TProgramAddress, TAccountMerchant, TAccountPlan> {
  // Program address.
  const programAddress = config?.programAddress ?? APP_PROGRAM_ADDRESS;

  // Original accounts.
  const originalAccounts = {
    merchant: { value: input.merchant ?? null, isWritable: false },
    plan: { value: input.plan ?? null, isWritable: true },
  };
  const accounts = originalAccounts as Record<
    keyof typeof originalAccounts,
    ResolvedInstructionAccount
  >;

  // Original args.
  const args = { ...input };

  const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
  return Object.freeze({
    accounts: [
      getAccountMeta("merchant", accounts.merchant),
      getAccountMeta("plan", accounts.plan),
    ],
    data: getUpdatePriceInstructionDataEncoder().encode(
      args as UpdatePriceInstructionDataArgs,
    ),
    programAddress,
  } as UpdatePriceInstruction<TProgramAddress, TAccountMerchant, TAccountPlan>);
}

export type ParsedUpdatePriceInstruction<
  TProgram extends string = typeof APP_PROGRAM_ADDRESS,
  TAccountMetas extends readonly AccountMeta[] = readonly AccountMeta[],
> = {
  programAddress: Address<TProgram>;
  accounts: {
    merchant: TAccountMetas[0];
    plan: TAccountMetas[1];
  };
  data: UpdatePriceInstructionData;
};

export function parseUpdatePriceInstruction<
  TProgram extends string,
  TAccountMetas extends readonly AccountMeta[],
>(
  instruction: Instruction<TProgram> &
    InstructionWithAccounts<TAccountMetas> &
    InstructionWithData<ReadonlyUint8Array>,
): ParsedUpdatePriceInstruction<TProgram, TAccountMetas> {
  if (instruction.accounts.length < 2) {
    throw new SolanaError(
      SOLANA_ERROR__PROGRAM_CLIENTS__INSUFFICIENT_ACCOUNT_METAS,
      {
        actualAccountMetas: instruction.accounts.length,
        expectedAccountMetas: 2,
      },
    );
  }
  let accountIndex = 0;
  const getNextAccount = () => {
    const accountMeta = (instruction.accounts as TAccountMetas)[accountIndex]!;
    accountIndex += 1;
    return accountMeta;
  };
  return {
    programAddress: instruction.programAddress,
    accounts: { merchant: getNextAccount(), plan: getNextAccount() },
    data: getUpdatePriceInstructionDataDecoder().decode(instruction.data),
  };
}
